colorscheme cycler

currently supports vim + alacritty + i3

== vim ==
install chriskempson/base16-vim
add to vimrc:
  if filereadable($HOME.'/.cache/cocyc/vim.vim') | source $HOME/.cache/cocyc/vim.vim | else | colorscheme base16-default-dark | endif

== alacritty ==
add to alacritty.toml:
  import = ["~/.cache/cocyc/alacritty.toml"]

== i3 ==
i'll document it later
